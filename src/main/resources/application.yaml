spring:
  application:
    name: forum-service

  datasource:
    url: jdbc:postgresql://cohort5860-elvi.h.aivencloud.com:26772/defaultdb
    username: avnadmin
    password: ${AIVEN_PASS}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true